# Skill: Manual Override Authoring

Purpose:
- Write safe, narrow manual overrides for rows that can be exposed only with explicit restrictions.

Use when:
- a generic official branch can be used safely only with extra guardrails

Required behavior:
- keep overrides narrow
- document the restriction clearly
- align override language with classifier exclusions and tests

Outputs:
- override entry
- restriction note
- matching test update
